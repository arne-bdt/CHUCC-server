# Maven build configuration
# Use batch mode (no download progress, cleaner output)
-B

# To enable verbose output, run: mvn clean install -X (overrides -q)
# To see INFO messages, run: mvn clean install (without -q, edit this file)
