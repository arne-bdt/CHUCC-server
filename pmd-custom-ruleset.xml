<?xml version="1.0"?>
<ruleset name="Custom PMD Ruleset - Unused Code Detection"
         xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0
                             https://pmd.sourceforge.io/ruleset_2_0_0.xsd">

  <description>
    Custom PMD ruleset focused on detecting unused code to match VS Code / Eclipse JDT behavior.

    This ruleset ONLY includes unused code detection rules:
    - UnusedLocalVariable
    - UnusedPrivateField
    - UnusedPrivateMethod
    - UnusedFormalParameter (optional, can be disabled)
    - UnnecessaryImport
  </description>

  <!-- ============================================================ -->
  <!-- UNUSED CODE DETECTION RULES (matching VS Code warnings)      -->
  <!-- ============================================================ -->

  <!-- Detect unused local variables -->
  <rule ref="category/java/bestpractices.xml/UnusedLocalVariable">
    <priority>3</priority>
  </rule>

  <!-- Detect unused private fields -->
  <rule ref="category/java/bestpractices.xml/UnusedPrivateField">
    <priority>3</priority>
  </rule>

  <!-- Detect unused private methods -->
  <rule ref="category/java/bestpractices.xml/UnusedPrivateMethod">
    <priority>3</priority>
  </rule>

  <!-- Detect unused formal parameters (function parameters) -->
  <!-- NOTE: This is disabled by default as it can have many false positives
       for interface implementations, overridden methods, and event handlers -->
  <!--
  <rule ref="category/java/bestpractices.xml/UnusedFormalParameter">
    <priority>3</priority>
    <properties>
      <property name="checkAll" value="false"/>
    </properties>
  </rule>
  -->

  <!-- Detect unnecessary imports (unused, duplicate, from java.lang) -->
  <rule ref="category/java/codestyle.xml/UnnecessaryImport">
    <priority>3</priority>
  </rule>

</ruleset>
